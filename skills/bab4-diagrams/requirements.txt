# Python dependencies for bab4-diagrams
#   pip install -r requirements.txt --break-system-packages
#   python -m playwright install chromium      # one-time browser download
#
# System dependency (recommended, for clean ERD/DFD layout & Chen notation):
#   sudo apt-get install -y graphviz           # provides `dot` and `neato`
python-docx>=1.1
playwright>=1.40
pillow>=10.0
openpyxl>=3.1
